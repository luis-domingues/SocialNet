@model List<SocialNet.Models.User>

@{
ViewData["Title"] = "Seguidores";
}

<div class="container mt-4">
    <h2 class="mb-4">Seus Seguidores</h2>

    @if (Model.Count > 0)
    {
    <div class="list-group">
        @foreach (var follower in Model)
        {
        <div class="list-group-item d-flex align-items-center">
            <div class="me-3">
                <i class="bi bi-person-circle" style="font-size: 2rem; color: #6f42c1;"></i>
            </div>
            <div>
                <h5 class="mb-0">
                    <a asp-action="ViewProfile" asp-route-userId="@follower.Id" class="text-decoration-none" style="color: #6f42c1;">
                        @follower.Username
                    </a>
                </h5>
                <p class="text-muted small mb-0">@follower.Email</p>
            </div>
        </div>
        }
    </div>
    }
    else
    {
    <p class="text-muted">Você ainda não tem seguidores.</p>
    }
</div>